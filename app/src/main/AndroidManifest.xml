<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.herdbook" >

    <application
        android:name=".ui.HerdBookApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HerdBook" >
        <activity
            android:name=".ui.animal.AnimalActivity"
            android:exported="false" />
        <activity
            android:name=".ui.herd.ScrollingActivity"
            android:exported="false"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/Theme.HerdBook.NoActionBar" />
        <activity
            android:name=".ui.gestation.GestationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.herd.HerdActivity"
            android:exported="false"
            android:label="@string/herd_view"
            android:theme="@style/Theme.AppCompat.NoActionBar" >
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" >
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>